# ═══════════════════════════════════════════════
# Aether — Python Dependencies (production, CPU-only)
#
# Install order matters. Install torch + torchaudio from the CPU
# index FIRST, then the rest:
#   python -m venv .venv && source .venv/bin/activate
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# ═══════════════════════════════════════════════

# ── Core ML ──
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.40.0
scikit-learn>=1.4.0

# ── Audio (Phase 1B voice) ──
librosa>=0.10.0
soundfile>=0.12.0
openai-whisper>=20231117
funasr>=1.0.0
modelscope>=1.14.0

# ── Data ──
numpy>=1.26.0
pandas>=2.2.0

# ── LLM / RAG (Phase 5) & Agentic (Phase 6) ──
groq>=0.11.0
langgraph>=0.2.0
langchain-core>=0.2.0

# ── Backend service (Phase 8) ──
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
requests>=2.31.0

# ── Dev / tests ──
pytest>=8.0.0
